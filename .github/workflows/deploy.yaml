name: Simple Test

on:
  workflow_dispatch:
    inputs:
      Target-Env:
        type: choice
        description: Target environment
        required: true
        options:
          - "dev"
          - "qa"
      setSecrets:
        type: boolean
        description: Set new secrets
        required: true
      key:
        type: string
        description: app id
        required: ${{inputs.setSecrets}}
jobs:
  infra-deployment:
    runs-on: ubuntu-latest
    env:
      Target: ${{ inputs.Target-Env }}
    steps:
      - name: Install Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run node
        run: npm run main
        env:
          App: testerMain

      - name: Other Action
        uses: ./.github/actions/deploy-secrets